package backend.KmlHandling;

import java.io.IOException;
import java.util.List;

public class IconListParser{
    List<String> iconList;

    public IconListParser(String kmlPath){
        List<String> iconList = null;

        //todo: declare buffered reader for the kml
    }

    //todo: read kml file as text until you get to <Folder> phrase - save it into one long string

    /*todo: read the newly created string. search for text between <Style id=" and the closest "
    * then parse them into iconList
    */
    
}
